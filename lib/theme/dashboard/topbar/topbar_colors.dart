import 'package:flutter/material.dart';

class TopBarColors {
  // --- Backgrounds ---
  static const Color background = Color(0xFF111827); // Deep Navy
  static const Color searchBg =
      Color(0xFF1F2937); // Dark Slate (Better than white for Dark Theme)
  static const Color sidebarLabelBg = Color(0xFF0B0F19);

  // --- Text Colors ---
  static const Color textPrimary = Color(0xFFF9FAFB); // Pure White
  static const Color textSecondary = Color(0xFF9CA3AF); // Muted Grey
  static const Color textBlack = Color(0xFF000000);
  static const Color systemText = Color(0xFF6EE7B7); // Soft Green for Status

  // --- UI Elements ---
  static const Color divider = Color(0xFF374151);
  static const Color searchShadow = Color(0x40000000); // 25% Black

  // --- Profile Section ---
  static const Color profileBg = Color(0xFF1F2937);
  static const Color profileBorder = Color(0xFF374151);

  // --- Status & Accents ---
  static const Color accentGold = Color(0xFFD4AF37); // Royal Gold
  static const Color notificationRed = Color(0xFFEF4444); // Tailwind Red
  static const Color systemOnline = Color(0xFF10B981); // Emerald Green
}
